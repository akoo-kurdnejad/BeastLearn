@page "{id}"
@using BeastLearn.Domain.Models.Permission
@model BeastLearn.Web.Pages.Admin.ManageRoles.EditRoleModel
@{
    ViewData["Title"] = "ویرایش نقشهای سایت";
    List<Permission> permissions = ViewData["Permissions"] as List<Permission>;
    List<int> selected = ViewData["Selected"] as List<int>;
}

<div class="row">
    <div class="col-lg-12">
        <h3 class="page-header">ویزایش نقشهای سایت</h3>
    </div>
    <!-- /.col-lg-12 -->
</div>
<form method="post" enctype="multipart/form-data">

    <input type="hidden" asp-for="Roles.RoleId" />
    <input type="hidden" asp-for="Roles.IsDelete" />
    <div class="row">
        <div class="col-lg-6">
            <div class="panel panel-warning">
                <div class="panel-heading">
                    نقش های سایت
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">

                    <div class="form-group">
                        <label>عنوان نقش</label>
                        <input type="text" asp-for="Roles.RoleTitle" class="form-control" placeholder="عنوان نقش">
                    </div>

                </div>


            </div>

        </div>

        <div class="col-lg-6">
            <div class="panel panel-warning">
                <div class="panel-heading">
                    دسترسی نقش ها
                </div>

                <div class="panel-body">

                    <ul>
                        @foreach (var item in permissions.Where(p => p.ParentID == null))
                        {
                            <li>
                                <input type="checkbox" name="SelectedPermission" @((selected.Any(p => p == item.PermissionId) ? "checked" : "")) value="@item.PermissionId" /> @item.PermissionTitle

                                @if (permissions.Any(p => p.ParentID == item.ParentID))
                                {
                                    <ul>
                                        @foreach (var sub in permissions.Where(p => p.ParentID == item.PermissionId))
                                        {
                                            <li>
                                                <input type="checkbox" name="SelectedPermission" @((selected.Any(p => p == sub.PermissionId) ? "checked" : "")) value="@sub.PermissionId" /> @sub.PermissionTitle
                                                @if (permissions.Any(p => p.ParentID == sub.ParentID))
                                                {
                                                    <ul>

                                                        @foreach (var sub2 in permissions.Where(p => p.ParentID == sub.PermissionId))
                                                        {
                                                            <li>
                                                                <input type="checkbox" name="SelectedPermission" @((selected.Any(p => p == sub2.PermissionId) ? "checked" : "")) value="@sub2.PermissionId" /> @sub2.PermissionTitle
                                                            </li>
                                                        }
                                                    </ul>
                                                }
                                            </li>
                                        }
                                    </ul>
                                }
                            </li>
                        }
                    </ul>

                </div>

            </div>


        </div>

    </div>
    <div class="row">
        <div class="container">
            <p>
                <input type="submit" value="ویرایش اطلاعات" class="btn btn-outline btn-warning btn-block" />
            </p>
        </div>

    </div>
</form>


